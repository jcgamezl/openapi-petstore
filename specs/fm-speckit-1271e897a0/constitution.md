# Project Constitution — `openapi-petstore`

> **Repository:** `https://github.com/jcgamezl/openapi-petstore`
> **Branch:** `master`
> **Generated by:** `constitution-generator` agent · Phase: `generate-constitution`
> **Modernization Score:** 42 / 100 (Medium Risk)

---

## 1. Project Identity

| Field | Value |
|---|---|
| **Name** | openapi-petstore |
| **Primary Language** | Java 8 (100 % of 16 source files, 988 LOC) |
| **Framework** | Spring Boot `2.0.2.RELEASE` |
| **Build System** | Apache Maven (`pom.xml`) |
| **Architecture Pattern** | Layered (API → Delegate → Repository) |
| **Contract Format** | OpenAPI 3 (`src/main/resources/openapi.yaml`, 820 lines) |
| **License** | See `LICENSE` |

---

## 2. Detected Principles

Each principle is grounded in a specific artifact found in the repository. Principles with **no supporting evidence** are explicitly excluded.

---

### PRINCIPLE 1 — API-First / Contract-Driven Development

**Evidence detected:**
- `src/main/resources/openapi.yaml` — 820-line OpenAPI 3 specification is the authoritative API contract.
- `.openapi-generator/VERSION` + `.openapi-generator-ignore` — Project was bootstrapped from OpenAPI Generator; the generator artifacts are committed.
- Delegate pattern files (`PetApiDelegateImpl.java`, `StoreApiDelegateImpl.java`, `UserApiDelegateImpl.java`) are named to implement generated interfaces, confirming the spec drives code generation.

**Rule:** The `openapi.yaml` specification is the single source of truth for all API shapes. Changes to request/response structure MUST start in `openapi.yaml`. Code is derived from the spec, not the other way around.

---

### PRINCIPLE 2 — Layered Architecture with Delegate Pattern

**Evidence detected:**
- `src/main/java/org/openapitools/api/` — Contains `PetApiDelegateImpl.java` (158 lines), `StoreApiDelegateImpl.java` (84 lines), `UserApiDelegateImpl.java` (280 lines), `ApiUtil.java`, and `ExceptionTranslator.java`.
- `src/main/java/org/openapitools/repository/` — Contains `HashMapRepository.java` (88 lines), `PetRepository.java` (48 lines), `OrderRepository.java`, `UserRepository.java`.
- `src/main/java/org/openapitools/configuration/` — Contains `OpenAPIConfiguration.java`, `SecurityConfiguration.java`, `ResourceServerConfiguration.java`, `AuthorizationServerConfiguration.java`, `HomeController.java`.
- Modernization analysis: `architecture_pattern = "layered"`.

**Rule:** Code MUST be organized in three distinct layers: `configuration` (Spring wiring), `api` (delegate implementations and exception handling), and `repository` (data access). Cross-layer dependencies must flow top-down only: `api` → `repository`. No layer may import from a layer above it.

---

### PRINCIPLE 3 — Security-First Development

**Evidence detected:**
- `src/main/java/org/openapitools/configuration/SecurityConfiguration.java` (30 lines) — Spring Security configuration.
- `src/main/java/org/openapitools/configuration/ResourceServerConfiguration.java` (30 lines) — OAuth2 Resource Server configuration; highest cyclomatic complexity file (max CC = 4).
- `src/main/java/org/openapitools/configuration/AuthorizationServerConfiguration.java` (31 lines) — OAuth2 Authorization Server configuration.
- Modernization blockers reference `spring-security-oauth2-autoconfigure:2.0.1.RELEASE`, confirming OAuth2 is an active, intentional dependency.

**Rule:** All API endpoints are protected by OAuth2. Security configuration MUST NOT be removed or bypassed. Any change to `SecurityConfiguration.java`, `ResourceServerConfiguration.java`, or `AuthorizationServerConfiguration.java` requires explicit justification and review. When upgrading Spring Boot to 3.x, the OAuth2 stack MUST be migrated to `spring-security-oauth2-authorization-server` or `spring-boot-starter-oauth2-resource-server` — not removed.

---

### PRINCIPLE 4 — Containerization Readiness

**Evidence detected:**
- `Dockerfile` — Present at repository root (build category).
- `src/main/resources/application.properties` (11 lines) — Externalized Spring Boot configuration, allowing environment-level overrides.

**Rule:** The application MUST remain buildable and runnable as a Docker container. The `Dockerfile` must be kept current with any changes to the build lifecycle (`pom.xml` packaging, Java version, entry point). Runtime configuration MUST flow through `application.properties` / environment variables — secrets must not be hardcoded.

---

### PRINCIPLE 5 — Centralized Exception Handling

**Evidence detected:**
- `src/main/java/org/openapitools/api/ExceptionTranslator.java` (29 lines) — Dedicated exception-to-HTTP-response translation class in the `api` layer.

**Rule:** All exception-to-HTTP status mapping MUST go through `ExceptionTranslator.java`. Individual delegate implementations (`PetApiDelegateImpl`, etc.) must not contain raw `try/catch` blocks that produce HTTP responses directly. New error conditions require a new mapping in `ExceptionTranslator.java`.

---

### PRINCIPLE 6 — RFC-Compliant Date Serialization

**Evidence detected:**
- `src/main/java/org/openapitools/RFC3339DateFormat.java` (22 lines) — Dedicated RFC 3339 date formatter at root package level.

**Rule:** All date/time values exposed via the API MUST be serialized in RFC 3339 format using `RFC3339DateFormat`. No other date format class or inline formatter may be introduced without replacing this as the canonical formatter in `OpenAPI2SpringBoot.java`.

---

### PRINCIPLE 7 — In-Memory Repository as Development Baseline

**Evidence detected:**
- `src/main/java/org/openapitools/repository/HashMapRepository.java` (88 lines) — Generic `HashMap`-backed repository base class.
- `PetRepository.java`, `OrderRepository.java`, `UserRepository.java` — All extend `HashMapRepository`.
- Data dependency analysis: `databases = []` — No persistent database connection detected.

**Rule:** The current storage implementation is intentionally in-memory (HashMap). Any introduction of a persistent data store (JDBC, JPA, NoSQL) MUST be introduced as a new `Repository` implementation behind the existing interface abstraction — not by modifying `HashMapRepository`. The in-memory implementation must remain available for local development and testing.

---

## 3. Detected Contradictions & Gaps

> These are observed discrepancies between what is present and what the detected principles imply. They are documented, not suppressed.

| # | Contradiction | Evidence |
|---|---|---|
| **C-1** | **CI/CD pipeline is incomplete.** `.travis.yml` (22 lines) is present, but `detect_tech_stack` returned `ci_cd = []`. Travis CI is configured but not detected as active or functional. | `.travis.yml` exists; `ci_cd` array empty |
| **C-2** | **Zero test coverage despite a "production" codebase.** `test_coverage_estimate = 0.0`. There is no `src/test/` directory. The security-first and delegate patterns imply testability, but no tests exist to validate them. | `get_metrics` result; file tree shows no `src/test` |
| **C-3** | **No pinned dependency versions.** `pom.xml` uses Maven property placeholders for Spring Boot and Jackson versions rather than explicit version pins. The modernization analysis explicitly flags this: *"Maven property placeholders prevent reproducible builds."* | `pom.xml`; modernization blocker #6 |
| **C-4** | **API surface scanner returned no endpoints,** despite three delegate implementation files (`PetApiDelegateImpl`, `StoreApiDelegateImpl`, `UserApiDelegateImpl`) and a full `openapi.yaml`. This suggests endpoints are defined in generated (or external) interfaces not fully visible to static analysis. | `get_api_surface` returned `endpoints = []`; `openapi.yaml` has 820 lines |
| **C-5** | **EOL dependency stack.** Spring Boot `2.0.2.RELEASE` (EOL May 2019), Java 8, and `spring-security-oauth2-autoconfigure:2.0.1.RELEASE` (EOL) are all in production use with no upgrade path present in the codebase. | `detect_tech_stack`; modernization blockers #1–3 |

---

## 4. Principles NOT Detected (Explicitly Absent)

| Principle | Why Excluded |
|---|---|
| **Test-First Development** | No test directory, no test framework configured, `test_coverage_estimate = 0.0` |
| **Code Style Enforcement** | No Checkstyle, PMD, SpotBugs, or formatting config detected in file tree |
| **Pinned Dependencies** | `pom.xml` uses property placeholders; reproducible builds not enforced (Contradiction C-3) |
| **Database Migration Discipline** | No Flyway, Liquibase, or equivalent detected; storage is in-memory only |
| **Type Safety (static analysis)** | No ErrorProne, NullAway, or annotation processor configuration detected |
| **Code Review Required** | No `CODEOWNERS`, no branch protection config, no PR template detected |
| **Continuous Integration Required** | `.travis.yml` present but not recognized as active by `detect_tech_stack`; no GitHub Actions or other CI detected (Contradiction C-1) |

---

## 5. Modernization Obligations (Derived from Score: 42/100)

These are not principles the codebase currently enforces but are **required before the next major version** based on the modernization assessment. They must become principles when work begins.

1. **Java 17 LTS upgrade** — Required blocker before Spring Boot 3.x migration.
2. **Spring Boot 2.7.18 intermediate step** — Required before jumping to Boot 3.x.
3. **OAuth2 stack migration** — `spring-security-oauth2-autoconfigure` → `spring-boot-starter-oauth2-resource-server`.
4. **Baseline test coverage ≥ 40%** — Must be established *before* framework upgrade to prevent regressions.
5. **Explicit version pinning in `pom.xml`** — All dependency versions must be pinned before upgrade work begins.
6. **Swagger UI upgrade** — `swagger-ui:3.14.2` → 5.x (minimum 4.x).

---

## 6. Package & File Ownership Map

| Package / File | Responsibility |
|---|---|
| `org.openapitools` (root) | Application bootstrap (`OpenAPI2SpringBoot.java`), date formatting (`RFC3339DateFormat.java`) |
| `org.openapitools.api` | API delegates, exception translation, API utilities |
| `org.openapitools.configuration` | Spring Security, OAuth2, OpenAPI/Swagger UI, Home routing |
| `org.openapitools.repository` | In-memory data access (`HashMap`-backed) for Pet, Order, User domains |
| `src/main/resources/openapi.yaml` | **Canonical API contract** — do not edit without API review |
| `src/main/resources/application.properties` | Runtime configuration (11 properties) |
| `Dockerfile` | Container build definition |
| `pom.xml` | Build lifecycle, dependency management |
| `.travis.yml` | CI pipeline definition (status: unconfirmed active) |

---

*This constitution was generated entirely from static analysis of `https://github.com/jcgamezl/openapi-petstore` (branch: `master`). No principles, files, or facts from any other repository were used.*